export enum PopupType {
  Alert = 0,
  Warning = 1,
  Error = 2,
}

export enum Position {
  Top = 'top',
  Right = 'right',
  Bottom = 'bottom',
  Left = 'left',
  Center = 'center',
}

export enum Size {
  XS,
  S,
  M,
  L,
  XL,
  Flex,
}

export enum Device {
  Unknown,
  Mobile,
  Tablet,
  Laptop,
  Desktop,
}
